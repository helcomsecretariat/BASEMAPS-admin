/**
 * Data access objects. It's a layer that communicates with database through Hibernate.
 * <p>
 * All Hibernate or plain SQL queries should be accommodated in these objects, data managers should use these objects in order to manage data entities (beans).
 * 
 * @author andrysta
 *
 */
package fi.fta.data.dao;