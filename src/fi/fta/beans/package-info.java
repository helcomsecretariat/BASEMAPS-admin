/**
 * The package contains all the beans used in business model and many of them are linked to database entities (tables). They also have data access objects respectively.
 * <p>
 * All database entities should be mapped in hibernate.cfg.xml
 *
 * @author andrysta
 *
 */
package fi.fta.beans;