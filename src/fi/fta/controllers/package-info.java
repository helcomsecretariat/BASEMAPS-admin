/**
 * Spring controllers managing web requests and giving responses.
 * <p>
 * See the request mapping paths in each controller and in each method to construct a URL for respective request.
 * Keep in mind that all URL's have prefix /sc (spring controller @see web.xml).
 * 
 * @author andrysta
 *
 */
package fi.fta.controllers;